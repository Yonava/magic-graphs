# @graph/create-graph

Factory function that assembles a graph instance by composing the core engine, plugins, and theme presets. The primary consumer-facing API for creating and configuring graphs.

**Key exports:** `createGraph()`
