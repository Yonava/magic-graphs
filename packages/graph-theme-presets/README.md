# @graph/theme-presets

Pre-built visual theme presets (dark, light, pink etc.) that supply tokens mapped to theme values for graph instances. Pass one or more of these to `createGraph()` to apply a consistent visual style.
