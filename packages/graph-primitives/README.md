# @graph/primitives

Foundational building blocks for the entire graph system. Defines `CoreNode`, `CoreEdge`, the event hub (with priority handling), transaction types, and the action/getter interfaces that core and plugins build on.
