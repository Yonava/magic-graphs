{
  "name": "magic-canvas",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "description": "the canvas layer for magic graphs",
  "dependencies": {
    "@vueuse/core": "^13.6.0",
    "tailwind-merge": "^3.3.1",
    "vue": "^3.5.18"
  }
}
