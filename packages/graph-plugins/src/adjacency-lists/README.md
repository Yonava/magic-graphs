# adjacencyLists

Derives adjacency lists from the graph, plain and weighted and direction-aware, as the shared substrate other analysis plugins build on.

| Export           | Dependencies | Optional dependencies |
| ---------------- | ------------ | --------------------- |
| `adjacencyLists` | none         | none                  |

**Controls:** `standard`, `weighted`, `directed`, `undirected`
