# animation

Captures before and after frames around a change so the canvas interpolates between them instead of snapping.

| Export      | Dependencies | Optional dependencies |
| ----------- | ------------ | --------------------- |
| `animation` | `canvas`     | none                  |

**Controls:** `auto`
