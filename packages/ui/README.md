# @magic/ui

Reusable Vue 3 UI components for the client application. Wraps PrimeVue with custom styling and provides form inputs (text, checkbox, range, color), popovers, icons, and composables like `useTextTip` and `useTinycolor`.
