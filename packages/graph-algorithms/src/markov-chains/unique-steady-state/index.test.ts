import { describe, test } from 'vitest';

// index.ts is still scaffolding, these are the behaviors to cover once it lands

describe('uniqueSteadyState', () => {
  test.todo('finds the steady state of an irreducible aperiodic chain');
  test.todo('returns a distribution whose entries sum to one');
  test.todo('returns a distribution left unchanged by the transition matrix');
  test.todo('reports that no unique steady state exists for a reducible chain');
  test.todo('reports that no unique steady state exists for a periodic chain');
  test.todo('does not mutate the input chain');
});
