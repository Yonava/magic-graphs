[build]
builder = "RAILPACK"

[build.config]
nodeVersion = "22"

[deploy]
buildCommand = "pnpm install && pnpm --filter client build && pnpm --filter server build"
startCommand = "pnpm --filter server start"

healthcheckPath = "/"
